# stembot-python

## Testing and Linting
1. Setup virtual environment
    - `python -m venv venv`
    - `source venv/bin/activate`
    - `pip install -e '.[build]'`

2. Run tests
    - `./test.sh`

3. Run linter
    - `pylint stembot`

